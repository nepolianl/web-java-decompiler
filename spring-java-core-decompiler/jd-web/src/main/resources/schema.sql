DROP TABLE source IF EXISTS CASCADE;
CREATE TABLE source (
	source_id BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	file_name VARCHAR(255) NOT NULL,
	class_name VARCHAR(255) NOT NULL,
	package_name VARCHAR(255) NOT NULL,
	html_text CLOB NOT NULL
);