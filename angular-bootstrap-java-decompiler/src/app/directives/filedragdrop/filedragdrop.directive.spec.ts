import { FiledragdropDirective } from './filedragdrop.directive';

describe('FiledragdropDirective', () => {
  it('should create an instance', () => {
    const directive = new FiledragdropDirective();
    expect(directive).toBeTruthy();
  });
});
